# =============================================================================
# KPI Dashboard - Production Environment Configuration
# =============================================================================
# Copy this file to .env.production and update values for your environment.
# NEVER commit the actual .env.production file to version control!
# =============================================================================

# Database Configuration
# ----------------------
# Generate a strong random password for production
POSTGRES_USER=kpiuser
POSTGRES_PASSWORD=CHANGE_THIS_TO_A_SECURE_PASSWORD
POSTGRES_DB=kpi_dashboard
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Backend Configuration
# ---------------------
NODE_ENV=production
BACKEND_PORT=4000
LOG_LEVEL=info

# JWT Configuration
# -----------------
# Generate a secure random string: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_AT_LEAST_32_CHARS
JWT_EXPIRY=7d

# Frontend Configuration
# ----------------------
# Update these URLs to match your production domain
NEXT_PUBLIC_API_URL=https://api.yourdomain.com
FRONTEND_URL=https://yourdomain.com

# Optional: SSL/TLS Configuration
# -------------------------------
# If using Let's Encrypt, these might be needed by certbot
# SSL_EMAIL=admin@yourdomain.com
# SSL_DOMAIN=yourdomain.com
